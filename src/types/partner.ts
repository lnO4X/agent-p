export interface Partner {
  id: string;
  userId: string;
  slot: number;
  name: string;
  avatar: string;
  modelId: string | null;
  definition: string;
  memory: string;
  createdAt: string;
  updatedAt: string;
}
